# Private Deposit

Research prototypes, experiments and benchmarks for a hashmap, where id's are public, but data remains private

In order to be able to run the proof testcases, run `create_circuits.sh` first once.
